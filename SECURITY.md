# Security Policy

## 数据边界

Health Tracker 处理健康数据和训练路线。请只在自己控制的设备与账户中部署：

- 不要把 Receiver 的 8787 端口映射到公网，也不要启用 Tailscale Funnel。
- 不要提交运行数据库、导出 JSON、Receiver `keys/`、S3/WebDAV 凭据、备份恢复密钥或 IPA。
- 云存储只应保存端到端加密包；它不是唯一备份。
- Receiver 的自动备份密钥应与 `.htbk` 备份文件分开保存。
- 精确路线属于高敏感数据；默认 Agent API 不返回完整路线。

## 发现泄露时

1. 立即撤销并重新创建对象存储 Access Key。
2. 停止 Receiver，保留数据库副本和日志，检查未知设备、序列缺口和异常对象。
3. 若 Receiver 私钥可能泄露，删除现有配对、轮换 Receiver 身份并重新配对手机。
4. 从 Git 历史中清除误提交秘密不能替代密钥轮换。

## 报告漏洞

请不要在公开 Issue 中附加健康数据、密钥、数据库或完整日志。创建不含敏感内容的 Issue 描述影响范围，或通过仓库所有者公开的私密联系方式报告。

## 医疗声明

本项目提供个人数据整理和趋势展示，不是医疗器械，不应用于紧急情况、诊断、治疗或替代医生意见。
